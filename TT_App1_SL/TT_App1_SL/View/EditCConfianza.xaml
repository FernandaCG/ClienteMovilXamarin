<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TT_App1_SL.ViewModel"
             x:Class="TT_App1_SL.View.EditCConfianza">
  <!-- <ContentPage.BindingContext>
        <viewModels:EditarCConfianzaViewModel/>
    </ContentPage.BindingContext>-->

    <StackLayout>
        <Entry Text="{Binding contacto.nombre}"></Entry>
        <Entry Text="{Binding contacto.apellitoPat}"></Entry>
        <Entry Text="{Binding contacto.apellidoMat}"></Entry>
        <Entry Text="{Binding contacto.celular}"></Entry>

        <Button Command="{Binding EditCommand}" Text="Edit CC"/>
    </StackLayout>
</ContentPage>